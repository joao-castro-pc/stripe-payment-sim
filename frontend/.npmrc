# openapi-typescript peers on TypeScript 5, but we use TS 6. The mismatch is
# harmless here, so accept it automatically instead of passing --legacy-peer-deps
# on every install (and so tools like the shadcn CLI can install cleanly too).
legacy-peer-deps=true
