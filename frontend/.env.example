# Copy this file to `.env` and fill in your Stripe TEST publishable key.
# Find it at https://dashboard.stripe.com/test/apikeys (the pk_test_... one).
# This key is public (safe for the browser) — but .env is gitignored anyway.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
